import React from 'react';
import '../Styles/Carousel.css';

const Carousel = () => {
  const myCarousel = [];
  console.log(myCarousel);

  return (
    <div>
      <h1>Carrousel</h1>
    </div>
  );
};

export default Carousel;
